# 技术债

| 债务 | 影响 | 优先级 | 偿还策略 |
| --- | --- | --- | --- |
| 传统用户/客户列表角色与工作区资源授权并存 | 授权路径多，新增接口容易漏掉工作区或所有权检查 | 高 | 所有新接口先走 `WorkspaceAccess`，再保留必要的 legacy 权限交集；补边界测试。 |
| 管理端仍是 Vue 2，邮件编辑器为独立 React 应用 | 前端依赖和状态模型存在双栈维护成本 | 低 | 变更时保持边界清晰；只有有迁移收益和测试覆盖时再评估合并。 |
| HTTP 全局无请求体/文件大小限制（`cmd/init.go` 未挂 `BodyLimit`，全仓无 `MaxBytesReader`） | 公开与受保护的 multipart/JSON 接口可被超大请求体打满内存或磁盘；ZIP/XLSX/附件均无服务端上限 | 高 | 在 Echo 全局中间件按接口分组挂 `BodyLimit`，并给 multipart 解析设置合理的 `MaxMultipartMemory`；为导入/媒体/事务消息附件单独设文件大小上限并返回 413。 |
| 导入临时文件不清理且无上传/解压上限（`cmd/import.go` 的 `FormFile`→无界 `io.Copy`→`os.CreateTemp`；`internal/subimporter/importer.go` 的 `ExtractZIP` 无未压缩大小/总量限制、解压目录不删除） | 每次导入/解压都向系统临时目录泄漏文件（含 ZIP bomb 与重复失败导入），长期运行可耗尽磁盘；CSV 文件句柄已于 2026-09-09 修复关闭 | 高 | 在会话结束与失败路径统一 `os.RemoveAll` 清理临时目录与文件；设上传/entry/总解压上限；补充失败路径与 ZIP bomb 测试。 |
| 媒体上传无解压/内存上限（`cmd/media.go` 的 `processImage` 直接 `imaging.Decode`） | 恶意或超大图片可造成大内存占用与 CPU 尖峰（解码+重采样），拖垮进程 | 高 | 上传前校验图片头/尺寸，限制最大像素数与文件大小；对解码增加超时与像素上限；异常时保持 `cleanUp` 清理逻辑。 |
| SES webhook 共享证书缓存存在数据竞争（`internal/bounce/webhooks/ses.go` 的 `s.certs` map 无锁读写，读 `:227` 写 `:255`；公共并发入口 `cmd/handlers.go:340-343`） | 并发合法 SNS 证书 URL miss 时 read/write 或 write/write 竞争，进程 `concurrent map` fatal 崩溃或返回不一致证书 | 中 | 用 `sync.RWMutex`/`sync.Map`/singleflight 保护 `certs` 并加缓存上限；为 `getCert`/`ProcessSubscription` 增加 `http.Client` 超时；补 `go test -race` 并发 getCert/ProcessBounce 测试。 |
| `BounceWebhook` 等处理器读取请求体无局部上限（`cmd/bounce.go` 第 217 行 `io.ReadAll(c.Request().Body)`，且匿名 `POST /webhooks/service/:service` 在鉴权/验签前读取；`cmd/handlers.go:340-343`） | 匿名大 body/并发请求可撑爆内存，造成堆耗尽、GC 压力与 worker 饥饿 | 高 | 用 `io.LimitReader` 限制读取量，或对 webhook 路由单独设置 `BodyLimit`；校验 Content-Type 与负载长度；补固定长度与 chunked 超大 body 在验签前被拒绝、并发压力测试。 |
| `SendTxMessage`/`/api/tx` 附件与收件人无上限（`cmd/tx.go` 第 61 行 `io.ReadAll(file)` 逐附件保留切片；`validateTxMessage` `:292-363` 只校验模式/地址；路由 `cmd/handlers.go:249`） | 持 `tx:send` 的租户可携带超大/大量附件与收件人，造成堆耗尽、长请求占用与渲染/队列/SMTP 放大 | 中 | 设置单附件/总附件/数量/recipient 上限，超限在分配前拒绝；用 `LimitReader` 包裹后再 `io.ReadAll`；补并发 queue saturation 测试。 |
| 退信队列无背压且 POP 扫描丢事件（`internal/bounce/bounce.go:138-140` 的 `Record` 对 1000 缓冲 `m.queue <- b` 阻塞发送；`Run()` 单 goroutine 同步执行 `RecordBounce` 的 DB 写；`mailbox/pop.go:187-197` POP 扫描用非阻塞 `select{case ch<-b: default:}`——队列满时丢弃 bounce，随后 `:201-205` 仍对已下载消息执行 `DELE`） | DB 变慢时 webhook 请求无限期阻塞（请求堆积/goroutine 泄漏）；邮件扫描路径则永久丢失 bounce/complaint 证据与抑制动作（数据丢失） | 高 | 统一背压策略：webhook 用非阻塞 select+丢弃/重试、POP 只在持久化成功后删除源邮件；为 `Record` 提供可取消 context；补饱和队列/回调失败/精确 accepted-dropped-deleted 计数测试。 |
| 大函数/多职责（`cmd/settings.go` 的 `UpdateSettings` 272 行、`cmd/tx.go` 的 `SendTxMessage` 271 行、`internal/manager/manager.go` 的 `worker` 191 行、`cmd/media.go` 的 `UploadMedia` 155 行、`internal/core/workspace_actions.go` 的 `CloneCampaignForWorkspaceWithSource` 163 行） | 分支多、意图混杂，改动易引入回归；缺少单元测试覆盖分支 | 中 | 按职责拆分：设置校验、收件人解析、媒体解析、状态机等独立成函数；补充针对分支的单元测试。 |
| 前后端存在重复的权限/工作区校验与 DTO 映射逻辑（`cmd/customers.go`、`cmd/campaigns.go` 等） | 授权规则漂移风险：后端多处重复“可读≠可发送/导出/修改”的判定，新增接口容易漏掉某一层 | 中 | 收敛到 `WorkspaceAccess` 与统一 helper，删除重复的 `require*` 分支；用边界测试锁定“不可导出/不可发送”行为。 |
| `cmd/handlers.go` 的 `initHTTPHandlers` 380 行路由表 | 路由/中间件堆叠混杂，权限边界难以审计 | 低 | 按资源域拆分路由注册文件，并在每个域内显式列出权限中间件。 |
| 工作区权限策略在 HTTP 与 Core 两层重复实现（`cmd/workspace_permissions.go:77/107/127/150` vs `internal/core/workspace.go:59/96/131/168/199/224`） | 已出现漂移：`cmd/campaigns.go:407` 拒绝不可复制请求后 `:425-429` 的 legacy 分支不可达；规则修复只落一层会越权或误拒绝 | 高 | Core 暴露唯一 policy decision（含 reason/capability），cmd 只叠加 legacy 角色/列表权限；删除不可达分支并补边界测试。 |
| Campaign 更新为非原子多步写入（`cmd/campaigns.go:442-580`，约 140 行 / 35 分支；`:524` 提交后 `:528` 写 mailbox、`:548-565` 删 pool 行、`:569-572` 逐个 attach 均独立事务） | 任一步失败留下部分更新；创建路径 `:311-393` 同步复制该流水线 | 高 | 拆 `prepareCampaignRequest` / `checkImmutableAudience` / `syncCampaignAudience`，由单个 Core 事务统一 campaign、mailbox 与 pool 关联。 |
| 公开收件人身份解析 CTE 在四个安全敏感路径复制（`queries/campaigns.sql:244-290` 渲染、`queries/customers.sql:367-406` 退订、`queries/links.sql:32-74` 点击、`queries/campaigns.sql:1579-1617` 打开） | 空 UUID 处理已有差异，同一 bearer 对不同事件路径的判定可能不一致 | 高 | 抽数据库函数或唯一 relation `resolve_public_recipient(campaign_uuid, customer_uuid)`，事件 SQL 只消费解析结果。 |
| Campaign 未发送计数至少复制 6 份（`queries/campaigns.sql:122-142/168-188/303-323/417-437/470-490/513-533`；`internal/core/workspace_queries.go:235-239/315-322/395-401` 再重写） | 真正发送状态 `campaigns.sql:1109-1150` 同时计入 pool 收件人，而多数投影不计 pool 或约束不同：UI、调度器、完成判定显示不同 unsent 数 | 高 | 抽唯一 `campaign_send_counts` 视图/CTE，列表、详情与调度器共享同一收件人有效集。 |
| 单/多 Campaign 收件人报表 SQL 几乎整段复制，并靠字符串替换注入权限（`queries/campaigns.sql:831-962` 132 行 vs `:964-1106` 143 行，规范化后 93 行相同；`internal/core/workspace_analytics.go:374-389` 依赖固定 `$11` 参数位置做 `strings.Replace`） | SQL 格式或 CTE 顺序变化可令权限改写静默失效；指标修复须同步两份 | 中 | 统一 `report_recipient_stats` relation，单/多入口只提供 campaign relation；scope 作为显式 CTE 输入，不做文本替换。 |
| 组织生命周期由多套大型事务硬编码资源表清单驱动（`internal/core/organizations.go:633-713`、`:722-837`、`:843-986`、`:1028-1108`；表集合分别在 `:755-779`、`:1069-1078`、`:1134-1142`） | 新增资源类型或修改 archive/transfer 规则时漏一个流程，留下孤立关系或可继续发送的数据 | 中 | 建立资源生命周期 registry 与显式 transition plan，拆 locks/validate、transition、merge、reset、media copy 各层。 |
| Customer 查询集合在 list/count/export/bulk/workspace 中多份维护且语义不一致（`queries/customers.sql:441-466` 列表/count 基本复制、`:474-521` export/raw template 再复制；`internal/core/workspace_subscriber_sql.go:39-60/94-111/154-177` 三份 membership 条件；`cmd/customers.go:670-782` 两个 55 行删除/拉黑 handler 同构、`:819-859` 复制两遍 membership switch） | 集合语义已漂移：`:449` 用 `customer_list_id = ANY(...)`，而 `:495/:518` 用 `= ALL(...)`，多名单/空数组行为不等价；列表、总数、导出和破坏性操作可能命中不同客户集合 | 高 | 建立唯一 `scoped_customer_ids` CTE/relation 承载 workspace/owner、名单成员、订阅状态、搜索与合法表达式；明确多名单语义为“任意”还是“全部”，统一 `ANY`/`ALL`。 |
| 前端存在无界轮询与脆弱的 iframe 握手（`frontend/src/App.vue:184` 每 500ms 轮询健康检查直到成功、无超时；`frontend/src/components/VisualEditor.vue:85-97` 每 100ms 轮询 10 次并 `setInterval`/正则替换 `&quot;`；`components/Editor.vue:170` 用 `innerHTML` 转换内容） | 应用重启故障时前端永久轮询；编辑器升级或模板含引号时渲染内容被破坏且难以调试 | 中 | 轮询加最大等待与失败回退；VisualEditor 改用项目 `onInit`/消息事件；转换逻辑移到 email-builder 侧并补充测试。 |
| email-builder 在 React render 阶段覆盖全局文档并永久追加订阅（`frontend/email-builder/src/App/index.tsx:41-51` 每次 render 调 `setDocument`/`subscribeDocument`；`documents/editor/EditorContext.tsx:35-37` 的 `subscribe` 丢弃返回值不 unsubscribe） | 抽屉状态变化触发重渲染即覆盖当前编辑内容为初始 props，且 onChange 回调随重渲染累积；StrictMode 开发态放大 | 高 | 初始化放入按 data 身份控制的 `useEffect`/`useRef`；订阅放 `useEffect` 并 return Zustand unsubscribe；避免模块级 store 被多实例共享或按实例创建 context。 |
| 活动只读权限未贯彻到实际编辑控件（`frontend/src/views/Campaign.vue:261` 传 `:disabled="!canEdit"`；`components/Editor.vue:69-76` 的 CodeEditor 与 plain textarea 未绑定 disabled，`:255-259` Ctrl+S 也无条件发更新） | 只读角色打开内容页仍可编辑本地内容并发无效写请求/403，误导“以为保存成功”（服务端仍是最终授权边界） | 中 | 所有编辑器统一接收 readonly，事件与快捷键也检查 canEdit；补只读角色 E2E。 |
| “先保存再启动/排期”保存失败后 Promise 永久 pending（`frontend/src/views/Campaign.vue:819-845` 手工 `new Promise((resolve)=>...)` 无 reject；`:872-888` 等它再改状态） | 4xx/5xx/断网时全局 toast 出现但流程永不 settle，按钮/状态卡死 | 中 | 直接 return API promise / async-await，传播 reject，并在 finally 恢复提交态。 |
| 同 model 并发请求用单布尔 loading，先完成者提前解除其余加载态（`frontend/src/api/index.js:39-40` 任一置 true、`:48-50` 任一置 false；`CampaignReport.vue:597-610` 同发 3-4 个全标 `models.campaigns`） | spinner/按钮过早恢复，允许重复操作并显示假完成 | 中 | 按 model 维护 in-flight 计数，或拆独立 request key。 |
| Campaign 未保存检测只比较 2 字段（`frontend/src/views/Campaign.vue:576-579` 只比较 body/contentType；表单另含 name/subject/lists/archive 等 `:480-514`；离页/刷新保护依赖它 `:1052-1078`；`beforeDestroy` `:1148-1150` 未清 `onbeforeunload`） | 绝大多数表单修改离页/刷新时静默丢失而不会弹出未保存提示 | 中 | 加载后存规范化完整快照/dirty flag，保存成功刷新基线；销毁时移除 unload handler。 |
| 全局初始化任一请求失败导致根应用不挂载（`frontend/src/main.js:59-65` 无降级 `Promise.all`；`:360` 顶层调用无 catch；mount 在 `:309-311`） | 任意瞬时失败（profile/config/orgs）即白屏，且无页面级重试 | 中 | 认证必需项与可降级项分阶段/`allSettled`；顶层 catch 渲染可重试错误页。 |
| 订阅表单生成器把持久化列表 name/description 直接拼入可部署 HTML（`frontend/src/views/Forms.vue:70-111`，尤其 `:82`、`:86` 无转义） | 若可写主体置入标签/脚本且管理员复制该片段部署到站点，形成存储型 XSS；需结合后端字段清洗与 CSP 验证可利用性 | 中（待验证） | 文本/属性上下文分别编码，用 DOM/template 生成后序列化；补 `< > & " '` 测试。 |
| 活动/分析域长组件与重复（`Campaign.vue` 1186 行、`CampaignAnalyticsReport.vue` 1055 行、`CampaignReport.vue` 820 行；两个分析组件共享 25 个方法/计算属性名；`CampaignReport.vue:320-679` vs `CampaignAnalyticsReport.vue:400-881` 重复权限/卡片/filters/loadAPI/分页） | 重复已产生权限实现差异：单活动报告用 `$canReadCampaignRecipients`（`:358-363`）、聚合报告手写组合条件（`:451-459`）；修改需同步多处 | 中 | 抽 report composable/service、统一权限 helper 与参数 builder；Campaign 拆 General/Content/Archive/Delivery 子表单。 |
| 组织选择存在跨请求竞态（`frontend/src/views/organizations/ManageOrganizations.vue:316-332` 读取可变 `selectedOrganizationID`、await 后无条件覆盖 members/invites；`:285-290` 切换触发刷新但无取消/令牌；`:345-353` 行操作使用当前 ID） | 快速切换 A→B 且 A 晚返回时展示 A 成员+B 上下文，后续操作可能附着 B 组织 ID；服务端仍以请求所属组织为边界 | 中 | 入口快照 organizationID，全部请求/行操作绑定该 ID；用递增 token 或 AbortController 丢弃过期响应。 |
| email-builder 对 URL hash 解码无错误边界（`frontend/email-builder/src/getConfiguration/index.tsx:4-12` 的 `atob`/`decodeURIComponent` 在 try 之外，解析后无 schema 校验、无 React ErrorBoundary；`documents/editor/EditorBlock.tsx:18-23` 缺块时直接 throw） | 畸形 `#code/` 或结构合法但缺引用的 JSON 可让独立构建白屏 | 中（待验证） | 解码+parse+schema safeParse 放入同一错误边界，限制 hash 大小，失败回退初始文档并提示。 |
| 表单生成器 JSON/元数据解析无捕获路径（`frontend/src/views/Settings.vue:115-137` 的 `:133` 直接 `JSON.parse`、`frontend/src/views/Campaign.vue:849-863` 的 `:857` 归档保存 `JSON.parse` 均无 try/catch；对照 `PersonalSMTPSettings.vue:301-320` 已有 try/catch+Array.isArray） | 非法 JSON 在提交前抛 SyntaxError，请求不发且无受控反馈，提交态卡死 | 低 | 抽共享 parse/validate helper，校验顶层结构并给字段错误。 |
| 高级客户 SQL 允许表未维持数据边界（`internal/core/subscribers.go:20-31` allowlist 含 `users/campaigns/bounces/campaign_views/links/link_clicks` 等全局表；`subscribers.go:708-734` 的 `validateQueryTablesWithArgs` 只用 EXPLAIN 验证“表名允许”，不要求子查询与当前 workspace 关联；`cmd/customers.go:133-155` 调用者表达式进入外层 WHERE） | 持 `customers:sql_query` 权限的 API 用户可用 `EXISTS(SELECT 1 FROM users WHERE username=... AND password LIKE 'a%')` 布尔推断跨工作区元数据；API 用户 `password` 为明文 legacy token，可能恢复凭据 | 中高（依赖权限分配） | 高级 SQL 功能本身是设计，缺陷是 allowlist 未维持数据边界；禁止 `users.password` 等敏感列、要求子查询经 workspace scope、拒绝函数型耗时表达式，并补“未关联敏感表拒绝/不可跨工作区推断/不可读 password”测试。 |
| 回信邮箱测试允许任意主机和端口（`cmd/handlers.go:264-268` 路由；`cmd/reply_mailboxes.go:133-180` 客户端控制 host/port → `pop3.New(...).NewConn().Auth()`；`validateReplyMailboxRequest` `:197-221` 只校验端口范围；`queries/replies.sql:23-32` 的 `update-reply-mailbox` 不重置 `status/verified_at`） | 认证用户可用其形成 SSRF/内网 TLS 端口探测（连接+认证错误差异可回显）；先验证受控 endpoint 再改 host/port 即可把内部目标设为 active 而无需重新验证，后台 Reply AI worker 周期连接 | 高（依赖部署） | 拒绝回环/RFC1918/链路本地/云元数据地址，解析后验证、防 DNS rebinding、统一错误与超时；更新连接参数后强制重新验证；补私网/元数据/重绑定测试。 |
| 回信 AI/转发 worker 每分钟全量下载邮箱、每封完整加载后才去重（`cmd/reply_ai.go:24-27,47-57,74-105` 60 秒扫描 `RetrRaw` 全量；`cmd/reply_forwarder.go:38-47,71-103,106-125` 同；去重键在完整 raw 载入后才计算且源消息不删除） | 向公开邮箱投递大邮件/大量邮件即可让每轮重复下载、解析、哈希，网络/CPU/内存/SMTP 队列持续放大 | 中高 | 每轮数量上限、单封大小上限、先 UID/摘要去重、超大 MIME/附件处理；补积压邮箱与慢服务器测试。 |
| OIDC 自动建号不检查 `email_verified`（`internal/auth/auth.go:25-31` 定义了 `EmailVerified` 但从未使用；`cmd/auth.go:203-257` 只校验 email 非空+格式；`createOIDCUser` `:419-447` 直接用 claims） | provider 允许未验证/可变 email claim 时，攻击者可用受害者 email 登录现有账户或创建错误绑定账户（账户接管/错误身份绑定） | 高（依赖 IdP claim 语义） | `email_verified=false` 必须拒绝；user-info fallback 同样验证；补已有用户碰撞与未验证 claim 测试。 |
| 匿名首次管理员设置存在 TOCTOU（`cmd/auth.go:84-93` 读 `a.needsUserSetup`（锁只保护布尔）；`doFirstTimeSetup` `:502-570` 创建 role/user 无 DB 级互斥，`:116` 创建后才置 false） | 并发两个 `/admin/login` POST 可各自建 role_id=1 用户并建立 session，首次部署时产生多个超级管理员 | 高（窗口窄） | DB advisory/table 锁或唯一 bootstrap 状态；并发 setup 仅一个成功；补并发测试。 |
| 回信转发的瞬时发送失败被去重表永久吞掉（`cmd/reply_forwarder.go:106-130` `ON CONFLICT DO NOTHING ... RETURNING id`；`:156-163` 失败置 `failed`；下一轮同 key 命中 `ErrNoRows` 直接 `return nil`；grep 无 retry worker 消费 failed/pending 行） | 队列拥塞/SMTP 暂时故障/reload 时客户回复永久不转发，状态仅留在 DB | 中高 | 增加失败行重试 worker（或消费 failed/pending）、重试上限与退避；补首推失败后下一轮重试与崩溃恢复测试。 |
| Postmark webhook 在用户名或密码任一项为空时 fail-open（`internal/bounce/webhooks/postmark.go:107-117` `len(u)==0||len(p)==0 → return true`；`cmd/init.go:824-848` 初始化无强制非空凭据；公开路由 `cmd/handlers.go:340-343`） | admin 若只设一项或保留空默认并开启，公开 endpoint 接受无认证伪造 bounce/complaint，可按配置触发 unsubscribe/blocklist | 中（依赖错误配置） | enabled+任一空凭据应初始化失败或请求拒绝；空 Authorization 不通过；补测试。 |
| session 清理仅执行一次而非周期执行（`internal/auth/auth.go:79,109-115` `auth.New` goroutine 调一次 `st.Prune()` 后只 `Sleep` 一次即退出，无 loop/ticker） | 长生命周期实例中过期 sessions 持续增长，增加 DB 存储与查询负担（非认证绕过） | 低 | 改阻塞 loop 或 ticker，补跨 interval 多次 prune 与错误后继续的测试。 |
| 回信 AI 只校验 `From` 头即可对任意客户执行退订/投诉（`cmd/reply_ai.go:118` 取 `From`、`:177` 用该邮箱匹配客户；`internal/core/reply_ai.go:61-83` 无 email 之外校验；无 envelope recipient/原始 campaign-recipient 关系/reply token/SPF-DKIM-DMARC 校验） | 攻击者向 campaign Reply-To 邮箱投递 `From: victim@example.com` 即可触发对受害者的 blocklist/unsubscribe/complaint，破坏性订阅变更无身份认证 | 高 | 验证发件人可信性（SPF/DKIM/DMARC 或仅接受含 token 的回复），校验与原始投递的对应关系；补伪造 From/认证失败/不匹配拒绝测试。 |
| Pool Reply AI 事件 lease 过期后仍可提交副作用（`internal/core/reply_ai.go:102-130` pool 分支仅 `SELECT EXISTS` 检查 lease（无 `FOR UPDATE`）；`:127` 终态 `UPDATE` 未检查 `RowsAffected` 即 `tx.Commit`；`:120` pool complaint 插入无 `reply_ai_event_id` 幂等键——对照 legacy 分支 `:163` 有 `ON CONFLICT (reply_ai_event_id) DO NOTHING` 且 `:137-141` `FOR UPDATE`） | 双 worker 租约竞争时，租约过期方仍可能插入重复 complaint bounce/exclusion 并提交，投诉计数重复或副作用越界；池路径无重试幂等 | 高 | pool 分支同样 `FOR UPDATE` 检查 lease；终态更新校验 `RowsAffected==1`；为 pool complaint 加事件幂等键；补双 worker reclaim 与 stale worker 不提交测试。 |
| singleton importer 并发准入非原子（`internal/subimporter/importer.go:200` `isDone()` 检查与 `:218` `im.Lock()` 之间有间隙；`cmd/import.go:29-35` 只检查 getStats+requireImportAccess） | 两个授权请求同时通过 `isDone()` 后各建 session，后者覆盖全局 status/owner/log buffer；两个 loader/consumer 可同时写入同一 singleton，其他工作区看到错误状态/日志，Stop 可能作用于错误导入 | 中高 | `isDone()`+状态置位放入同一临界区（或 CAS）；用 barrier 测试并发 `NewSession` 单胜者、不同工作区并发导入状态/日志/Stop 归属、`-race` 测试。 |
| 回信 AI POP3 轮询为单 goroutine 串行且无读超时（`cmd/reply_ai.go:47-107` 一个 goroutine 依次扫描全部邮箱；`go-pop3.Opt` 仅提供 `DialTimeout`，`NewConn/Auth/RetrRaw` 无读超时或 context deadline） | 单个 POP 服务器变慢/挂起会无限期阻塞唯一处理器，所有回信邮箱的分类与事件处理全部停摆（区别于 #38 的重复下载放大，此为本条聚焦的存活性问题） | 高 | 为每个邮箱连接设读超时或 context deadline，将各邮箱扫描放入有界并发 worker 池；补慢服务器/挂起连接下其余邮箱仍被处理的测试。 |
| 公海“活跃且未剔除”成员判定在 Go 中复制 3 份且 upsert 语义不一致（`internal/core/pools.go`：`ResolvePoolRecipients` 628-634、`resolvePoolRecipientsForSegment` 642-648、`AttachPoolToCampaign` 729-731 内联；`EnsurePoolCampaignRecipients` 615 用 `DO UPDATE`，`AttachPoolToCampaign` 718/731 用 `DO NOTHING`） | 成员资格形状重复三处，新增剔除来源或状态只改一处会静默漂移，导致向已剔除/已移除联系人发送，或快照无法刷新 | 高 | 抽唯一 `activePoolSegmentRecipients` 查询/relation 供三处消费；统一 upsert 语义；补边界测试断言三条路径产出相同收件人集合。 |
| 回信 AI 的公海联系人解析在重复邮箱/多次投递下取任意一条（`internal/core/reply_ai.go:40-56` 按 `LOWER(email)` 匹配后 `ORDER BY cpr.created_at DESC LIMIT 1`；`pool_contacts.email` 无唯一约束（`schema.sql:852` 仅普通索引），同一邮箱可对应多个 contact 与多次投递） | 同一组织内邮箱命中多个公海投递时，退订/投诉写入的 `pool_segment_exclusions` 落到“最近快照”而非实际投递，可能排除错误的池/组织 | 中 | 用外向 Message-ID 或每收件人 reply token 把回信关联到具体投递；无法唯一匹配时按歧义审计并拒绝，而非 `LIMIT 1`。 |
| 公海与回信 AI 的 junction/审计列缺少外键（`schema.sql:927-940` `campaign_pool_recipients` 仅 `campaign_id` 有 FK；`schema.sql:646-675` 与 `internal/migrations/v6.27.0.go:118-121` 的 `reply_ai_events.pool_contact_id/pool_id/source_segment_id/source_organization_id` 为裸整数列） | 一旦出现联系人/段/组织删除路径，快照与审计行将悬空，回信审计 join 静默漏行或错归属；`ON DELETE` 语义对这些边未定义 | 低 | 为 `campaign_pool_recipients` 与 `reply_ai_events` 的 pool_contact_id/pool_id/segment_id/organization_id 补 FK（或显式记录去规范化并配清理任务），并补删除路径的迁移测试。 |
