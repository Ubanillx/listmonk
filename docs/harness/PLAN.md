# 开发计划

本文件记录可执行的后续工作。计划不是承诺；合并前应补充 issue、负责人和验收标准。

## 持续维护与兼容性

1. AI 入站回信分类已完成；后续仅维护模型/邮箱配置、租约重试和审计回归。
2. 认证兼容窗口已结束：移除 v3→v4 BasicAuth/session Cookie 特例，并完成旧会话冲突回归；模板主题迁移与 opt-in 私有列表已实现并有回归测试。
3. 为工作区资源的 `read`、`use`、`copy`、`manage` 边界持续补充 API 与事务并发测试。
4. 已在当前环境和 CI 检查清单执行 `cd docs/docs && mkdocs build --strict --clean`；后续仅维护依赖和链接。

## 已实施：媒体逻辑文件夹（v6.37.0）

- [x] `media_folders` 表、媒体 `folder_id`、根目录语义、工作区唯一目录名约束和幂等升级迁移。
- [x] 目录查询/创建/改名/空目录删除/嵌套移动、媒体移入目录接口；沿用 `media:manage`，并在 Core 事务内复核工作区、成员资格、所有权和循环边界。
- [x] 管理端面包屑、当前层级目录网格、拖拽媒体/目录、本地文件拖拽上传、多语言文案和媒体样式。
- [x] API 文档、架构台账、业务不变量及名称/工作区边界测试同步更新。

来源：`cmd/media.go`、`cmd/media_folders.go`、`internal/core/media_folders.go`、`internal/media/media.go`、`schema.sql`、`internal/migrations/v6.37.0.go`、`frontend/src/views/Media.vue`。

## 已实施：业务审计日志（v6.32.0）

> 实现进度（2026-09-15）：新增 `audit_events` 与幂等迁移 `internal/migrations/v6.32.0.go`，加入 `audit:get` 权限、当前工作区服务端分页查询 API、选择/全量 CSV 导出和管理端审计页。认证 API 中间件覆盖活动、客户/名单、导入、退信、模板、SMTP、回复邮箱/转发、事务邮件及导出等核心动作；活动发送器、退信 webhook、回信转发和回信 AI 补记后台生命周期。打开/点击等高频事实保留在原表，审计元数据禁止密码、令牌、正文和附件。当前写入为业务提交后的 best-effort；需要同事务强一致时复用 `RecordTx`。

来源：`schema.sql`、`internal/audit/`、`cmd/audit.go`、`internal/manager/`、`cmd/reply_*.go`、`cmd/bounce.go`、`frontend/src/views/Audit.vue`。

### 已实施：审计日志查询与导出（2026-09-15）

- `GET /api/audit-events` 由服务端执行筛选、总数统计和分页，默认每页 50 条、上限 100 条，避免管理端加载全量日志。
- `GET /api/audit-events/export?scope=selected&ids=...` 导出当前工作区内选中的日志；`scope=all` 按当前筛选条件导出全部匹配记录，服务端以 CSV 流式返回，不受当前分页限制。
- 两种导出均复用工作区边界和筛选条件；选择导出最多 1000 个 ID，前端选择范围为当前已加载页，跨页全量需求使用“导出全部”。导出请求由 `audit.exported` 留痕。
- 验证已覆盖分页总数/页数据、个人空间与组织空间隔离、跨工作区详情拒绝、选择/全量 CSV 内容及附件响应头，并通过 Go 全量测试、真实 PostgreSQL 集成测试、前端 lint/build。

来源：`cmd/audit.go`、`cmd/handlers.go`、`frontend/src/api/index.js`、`frontend/src/views/Audit.vue`、`cmd/audit_integration_test.go`。

### 已实施：第二阶段业务与安全审计覆盖（2026-09-15）

目标是把订阅/隐私自助、公海及邮件资产、账号与组织安全这三组低频业务操作纳入同一套工作区隔离审计模型；每个成功、失败或权限拒绝请求都能按稳定动作名、操作者、对象、结果和固定原因码检索，且不泄露密码、令牌、邮箱正文或完整地址集合。

- [x] 订阅、退订、Opt-in、客户自助导出和数据擦除：公共请求使用 `customer` 操作者并尽量绑定工作区与客户 UUID；失败场景保留 HTTP 状态/固定原因码。
- [x] 公海联系人：新增、清除邮箱、普通列表导入、公海分配授权、二级成员导入、分配、剔除、恢复；批量导入写入数量、冲突、未匹配和无效统计，不写邮箱。
- [x] 媒体与自定义字段：上传、删除、创建、更新、停用，媒体只记录安全的类型/扩展名/缩略图摘要。
- [x] 用户、角色、组织、API Key、登录安全：用户/批量用户、角色、成员/邀请、组织资源转移/归档/删除、Key 创建/更新/轮换/撤销、2FA、登录成功/失败、登出、密码重置和 OIDC 结果。
- [x] 目标路由注册完整性、用户/API Key/customer 操作者识别、公共自助上下文、成功/403/失败结果和敏感字段边界测试；通过 Go 全量测试、真实 PostgreSQL 审计集成测试、前端 lint/build，并完成 Docker 重启验证。

实现入口：`cmd/audit.go` 的稳定动作与中间件，`cmd/public.go`、`cmd/pools.go`、`cmd/media.go`、`cmd/custom_fields.go`、`cmd/users.go`、`cmd/roles.go`、`cmd/organizations.go`、`cmd/api_keys.go`、`cmd/auth.go` 的业务上下文补充；不改变原业务事务结果，审计写入仍为 best-effort。

## 下一步：业务审计日志测试与覆盖补齐

> 计划更新（2026-09-15）：第一阶段实现已完成，专项测试已补齐到可作为 P0 回归门槛；当前仍不宣称全系统写操作覆盖。真实 PostgreSQL 集成测试通过 `AUDIT_TEST_DSN` 与 `MIGRATION_TEST_DSN` 启用，默认 `go test ./...` 不依赖外部数据库。

### P0.1 审计专项测试门槛（已完成）

- [x] 审计 Writer 真实 PostgreSQL 写入：字段、默认值、IP/字符串规范化、元数据和异常输入。
- [x] `RecordTx` 提交/回滚行为。
- [x] v6.32.0 迁移幂等、旧历史 `organization_id=NULL` 归一到个人空间 `0`、索引和非空约束。
- [x] HTTP 中间件成功、403 拒绝、用户/API Key 操作者、请求 ID、稳定动作名和对象 ID。
- [x] 审计查询个人工作区隔离及跨工作区详情读取拒绝。
- [x] 审计查询服务端分页，以及当前筛选条件下的选择导出/全量 CSV 导出。
- [x] 认证 API 审计路由注册完整性检查。
- [x] 活动发送完成、发送错误暂停、后台失败原因的生命周期回调断言。
- [x] `go test ./...`、真实 PostgreSQL 审计集成测试、前端 lint/build。

### P0.2 剩余邮件异步与治理覆盖（下一步）

- [ ] 为异步客户导入增加 `started/completed/failed/stopped` 终态事件，并记录任务 ID、处理数量和安全原因码。
- [ ] 为 webhook 退信按工作区聚合事件，保留现有 `bounces` 逐封事实表，避免在审计表写入高频明细。

### P1 安全与组织管理审计

- [x] 用户、角色、组织成员、邀请、组织资源转移/归档/删除（第二阶段已完成）。
- [x] API Key 创建、轮换、撤销，2FA 启用/停用，登录、登出、登录失败和权限拒绝（第二阶段已完成）。
- [ ] 为上述事件补充业务原因码、前后状态摘要和权限变更前后差异；继续禁止密码、令牌、正文和附件进入元数据。

### P2 运维与治理

- [ ] 审计保留期、归档/清理策略和平台管理员跨工作区查询规则。
- [ ] 按动作、操作者、结果、工作区建立运营统计和失败告警。
- [ ] 增加 Cypress 页面回归：权限显隐、分页筛选、详情展开、脱敏和空状态。

### 验收门槛

- 剩余异步导入/导出与退信聚合完成前，不将审计日志描述为“全系统写操作全量覆盖”；每新增业务写操作必须同时更新 `auditRoutes`、覆盖矩阵和至少一个成功/失败测试。
- P1 上线前，必须通过跨工作区隔离、敏感字段扫描和 API Key/用户操作者识别测试。

## 已实施：一级公海与组织公海分配

> 规则补充（2026-09-15，2026-09-16 修订）：公海数据导入已收敛到统一客户导入入口。选择单个一级 `pool` 列表时，服务端读取首个 CSV/XLSX 工作表的客户编号、姓名、邮箱和分配部门；支持模板中文表头及字段映射，其他模板列忽略，分配部门必须匹配启用中的组织名称，未知部门的行报错并跳过写入，不自动创建组织。若对应公海分配已存在，合法联系人自动加入；若公海分配后创建，创建事务回填已有同部门联系人。公海导入与公海管理写操作均只对最高管理员开放；公海管理窗口只负责目标组织选择及公海分配创建/绑定，历史专用导入路由仅保留兼容性。

> 实现进度（2026-09-07）：已完成 v6.27.0 数据迁移、模型、API、活动公海关系、组织级逻辑剔除、按编码查询、发送快照与回件邮箱解析；公海分配统一通过“一级公海拆分并绑定”创建，通用客户列表不再允许 `org_pool_allocation`，二级合并一级及待绑定接口已移除。已补齐公海消息页、退订/退信/回复 AI 的组织隔离回写、活动选择器授权入口、联系人/公海分配运营管理界面和回件邮箱配置。每个“一级公海 × 组织”保持一个公海分配以唯一解析回件邮箱。已完成 fresh schema、开发 PostgreSQL 迁移验证、Go/前端构建与 MkDocs strict 构建，并在 Docker 栈执行 API、浏览器权限矩阵和真实 MailHog 投递回归。公海需求及 v3→v4 认证兼容清理均已完成。

来源：2026-09-04 用户确认的客户联系方式防泄露、公海投放和组织分配需求。

### 已确认的业务规则

- 新增独立的一级公海列表类型；现有 `public` 类型继续仅表示允许匿名订阅，不能复用为公海。
- 一级公海联系人是独立的全局主数据，保存导入的客户编码、公司名称和真实邮箱。客户编码允许重复，不能作为数据库主键、去重键或写操作的唯一定位条件。
- 一级公海和组织公海分配对所有非最高管理员只返回客户编码、公司名称、状态与脱敏邮箱；真实邮箱不得出现在列表、详情、复制控件、CSV、单客户资料导出、API Key 响应或浏览器状态中。
- 一级公海始终保留完整成员。公海分配是某一级公海成员分配给一个组织的受控子集，引用公海联系人而不复制真实邮箱或形成独立客户记录。
- 最高管理员按“一级公海列表 x 组织”单独授予投放权限。获得投放权限的组织可在活动中选择一级公海，即使没有该一级列表的查看权限；服务端仅在生成投递快照时解析真实邮箱。
- 最高管理员在一级公海管理窗口显式选择目标组织；这是分配归属，不是加入组织或切换当前工作区。公海分配候选和新建列表按该目标组织读取或写入，内部回件邮箱由组织管理员在组织工作区维护。
- 活动可选择一级公海、公海分配或两者；同一公海联系人在单次活动中必须按公海联系人内部 ID 去重。
- 非最高管理员不能导出一级或公海分配的真实邮箱。按导入客户编码搜索时可返回多个候选记录，操作人从仅含安全字段的结果中选定系统内部记录后执行维护动作。
- 公海分配只能从一级公海拆分并绑定；统一客户导入入口选择单个一级公海后进入专用分支。模板必须提供客户编码、姓名、邮箱和分配部门；分配部门必须匹配启用中的组织名称，未知部门的行不得写入；导入的客户编码可重复；仅完整规范化记录完全一致时可视为同一条记录并幂等处理，同一客户编码但任一受比较字段不一致时保留为不同记录并写入冲突审计，绝不按客户编码自动合并、生成编码或覆盖导入值。分配部门不会自动创建组织；已有公海分配会自动接收匹配联系人，后创建的公海分配会回填匹配联系人。
- 无效联系人采用“剔除”而非物理删除；最高管理员可检索和审计被剔除记录。一级公海主数据及历史投递记录必须保留。
- 剔除是组织二级分配范围内的状态：只从该组织获分配的公海分配移出，不影响一级公海主数据、其他组织的二级分配或其投放资格。客户回复、明确退订和投诉也按实际投递来源所属的组织二级分配执行同一逻辑剔除。一级公海被该组织直接选为活动受众时，服务端仍按该组织的二级分配及剔除状态解析收件人，防止用一级选择绕过剔除。
- 被剔除记录不物理删除：最高管理员可跨组织查看完整审计；组织管理员仅可查看本组织二级分配范围内的剔除安全字段（客户编码、公司名称、状态、脱敏邮箱、原因和时间），不能借此恢复真实邮箱或重新加入投放范围。
- 组织在公海分配中手动移除客户时，必须在一级公海联系人上留下该组织维度的“已剔除/未分配”标记；这不是全局移除。该组织后续即使在营销活动中选择一级公海，也不得再次发送该客户；其他组织的二级分配、投放和剔除状态不受影响。一级公海界面向最高管理员展示各组织的剔除标记，组织用户只展示本组织标记。
- 公海分配可配置一个统一回件邮箱，且由所属组织管理员维护；不同组织的公海分配分别保存各自的回件邮箱。活动选择公海分配时使用该列表邮箱；活动选择一级公海时，服务端按目标组织的有效二级分配为每个联系人解析最终回件邮箱，优先使用对应公海分配邮箱，不使用一级公海的默认邮箱。回件邮箱是公司内部地址，不属于客户联系方式，组织用户和最高管理员均可直接查看完整地址，但最高管理员不在公海拆分弹窗代配。活动配置、预览、发送快照和回信审计必须明确标注“一级公海受众 / 公海分配回件邮箱（组织）”的来源。

### 实施顺序

- [x] 先更新 `docs/ARCHITECTURE.md`、`TECH_ARCHITECTURE.md` 与 `BUSINESS_LOGIC.md`，定义公海主数据、一级/二级关系、投放授权、剔除状态及发送/回复不变量。
- [x] 增加迁移：客户列表类型增加公海/二级语义；创建公海联系人、一级成员、组织二级分配、组织一级投放授权、组织级剔除和审计表。所有关联按内部 ID 建立外键与唯一约束，导入客户编码仅建立普通检索索引。
- [x] 建立 Core 事务：创建/导入一级公海、从一级拆分并绑定公海分配、从一级选择成员分配给组织、撤销分配和逻辑剔除。每个写入在锁定一级、二级、组织授权和联系人后重验权限及状态。
- [x] 收口权限与数据传输对象：最高管理员是唯一真实邮箱明文读取/导出主体；组织只能读取获分配公海分配的业务字段。一级投放授权是 `use` 权限，不能推导出 `read`、`export` 或 `manage`。
- [x] 改造活动受众与投递快照：活动关联支持本地客户列表、一级公海和公海分配；发送时由服务端解析真实邮箱，按公海联系人内部 ID 去重，过滤目标组织的手动剔除/回复剔除记录，并将来源一级/二级、组织、最终回件邮箱及解析规则保存到快照以支持退订、回复和审计。活动统计/详情响应同步返回内部回件邮箱来源，编辑页显示一级→二级路由。
- [x] 改造客户界面、客户列表和活动编辑器：统一客户导入入口按列表类型切分普通/公海字段；公海管理只保留目标组织选择及公海分配创建/绑定；一级仅在活动选择器以最小元数据显示给获授权组织，并在活动中显示每个受众最终使用的二级回件邮箱来源。
- [x] 收口所有敏感出口：列表/详情/API、CSV、单客户隐私导出、活动统计/收件人接口、退信和回信审计，以及 API Key scope。前端隐藏出口是体验层，Go 服务端拒绝或脱敏是唯一安全边界。
- [x] 增加 Go、API 与 Cypress 回归：跨组织读取、无一级查看权但可投放、一级/二级去重、导出拒绝、编码重复、普通列表导入幂等、冲突审计、二级手动移除在一级的组织标记、一级活动过滤剔除客户、组织间剔除隔离、统一回件邮箱解析与展示、缺失/冲突邮箱处理、回复后剔除及最高管理员明文例外均已覆盖；队列 SQL 与快照去重已有静态/数据库验证，真实 MailHog 投递已验证 3 个唯一联系人、回件路由和活动计数；Cypress 已覆盖获授权组织管理员的客户邮箱脱敏/内部回件邮箱明文、最高管理员明文例外，以及未获授权组织无公海入口和一级详情 403。

### 已确认的回复剔除不变量

客户回复、明确退订和投诉只能影响实际投递来源所属组织的二级分配。它们不得删除或改变一级公海主数据，不得影响其他组织；最高管理员必须能在剔除记录中查看来源活动、组织、公海分配、动作、时间和原因。一级公海直接投放只面向已分配给目标组织的成员，并同样尊重该组织的二级剔除状态。

### 已确认的发送解析细节

- 同一组织下，同一一级公海联系人禁止重叠归属；约束为 `(一级公海, 组织, 公海联系人)` 只能有一个有效二级归属。需要重叠的未来场景必须改为活动显式选择公海分配，不能静默猜测回件邮箱。
- 活动选择一级公海但目标组织没有有效二级归属或回件邮箱时，允许保存草稿并显示“无可用公海分配/回件邮箱”，但预览和发送必须阻断；不得回退一级或系统默认邮箱。
- 回件邮箱在活动配置、预览、发送快照和回信审计中显示公海分配名称、组织名称、邮箱显示名及完整内部回件地址；该地址不脱敏，但不得与客户真实邮箱字段混用或作为客户数据导出字段。

## 排定前检查

- 明确是否影响 `cmd/`、`internal/core/`、`internal/auth/`、`frontend/` 或数据库迁移。
- 先更新 [TECH_ARCHITECTURE.md](TECH_ARCHITECTURE.md) 和 [BUSINESS_LOGIC.md](BUSINESS_LOGIC.md) 中受影响的边界，再实现代码。
- 运行 `make test`、受影响的前端测试和部署脚本校验，并把结果写入 [STATUS.md](STATUS.md)。

## 退信邮箱 POP 检测（2026-09-08）

来源：用户已确认按推荐方案实施。范围：只读最后一封邮件、标准 DSN/MIME 解析、三种连接模式、四步检测结果；沿用已有设置权限与凭据行为。

- [x] 独立只读检测接口和标准退信摘要解析。
- [x] POP3 / SSL/TLS / STARTTLS，旧配置兼容和后台 STLS 适配。
- [x] 设置页检测按钮、结果卡片及 en/zh-CN/zh-TW 文案。
- [x] POP 模拟服务器、解析与配置校验测试，浏览器用例。
- [x] 最终浏览器回归、Docker 健康验证与状态记录。结果见 STATUS.md。

## 角色权限细分（2026-09-16）

目标：以业务操作为中心，在不削弱工作区、所有权、归档和 API scope 边界的前提下，把客户、营销活动和直接导出动作从粗粒度角色权限中拆出，并让角色编辑器显示可理解的中文/英文名称。平台管理员权限保持全量，不拆分用户、角色、设置和组织平台管理。

- [x] 新增客户删除/黑名单/列表归属、活动发送/收件人明细、退信删除/黑名单、直接客户导出、用户集成令牌和平台组织管理权限。
- [x] 新增客户导出/敏感字段、活动测试/排期/运行控制权限；客户列表成员维护沿用 `customers:membership_manage`，不重复新增同义权限。
- [x] 将事务消息发送移入独立的事务消息组；保留原权限 ID，避免已有角色和 API 兼容性变化。
- [x] 用 `v6.36.0` 为历史宽权限角色补齐细分权限；新角色默认关闭高风险动作，管理员可在角色中单独撤销。
- [x] 用 `v6.38.0` 为历史业务角色补齐新增动作权限，并让删除/黑名单/列表归属/活动控制在服务端按专用动作权限执行。
- [x] 组织平台管理使用独立路由门和管理路径白名单，不把平台操作员变成组织成员或普通资源管理员。
- [x] 直接客户/黑名单导出在 HTTP 请求阶段重验权限、工作区、成员资格、所有权、脱敏和公海明文限制；使用 `customers:export`，不创建任务或持久化文件。
- [x] 补齐 `permissions.json`、三份主要语言包、角色表单标签、业务/技术/用户文档与后端边界测试。
- [ ] 设置项内部的“查看、修改、维护、探测/测试”继续保持现有三档权限；若要进一步拆分 SMTP、回复邮箱、AI 探测等设置，应单独设计配置项级权限和兼容迁移。

## 文档补齐（2026-09-16）

来源：2026-09-16 文档核对，把 `docs/docs/content/`、`docs/ARCHITECTURE.md`、`docs/harness/` 与 `cmd/`、`Makefile`、`dev/`、`.github/workflows/` 逐项比对。

已完成（同一日期）：

- [x] 未文档化的 API 路由族。新增 6 个 API 页面覆盖此前无文档的全部 16 个 `/api/*` 前缀：`apis/organizations.md`（`/api/organizations/*` 28 条 + `/api/workspace`）、`apis/system.md`（health/config/lang/about/dashboard/custom-fields/events/logs/admin reload/maintenance/logout）、`apis/roles.md`、`apis/settings.md`、`apis/audit-events.md`、`apis/public-endpoints.md`（`/api/public/*`、`/api/media/file/*`、`/api/org-pool-allocations/*` 别名）；均已挂入 `mkdocs.yml` 的 API 分组。
- [x] `apis/customers.md` 端点表补上 `GET /api/customers/:id/activity`、`GET /api/customers/export`、`PUT /api/customers/query/customer-lists` 三个已实现端点。
- [x] `configuration.md` 新增 `## Configuration file reference`：逐段列出 `[app]`、`[db]`、`[privacy]`、`[security]`（含 oidc/captcha）、`[appearance]`、`[upload]`、`[bounce]`、`[smtp]`、`[[messengers]]`、`[reply_ai]`、`[maintenance.db]` 的键、类型、默认值与必填性。
- [x] 文档 CI 触发面：新增 `.github/workflows/docs-sanity.yml`（PR 的 opened/synchronize/reopened，paths `docs/**` 与 `scripts/check_docs.py`），执行 `scripts/check_docs.py` 与 `mkdocs build --strict`，补上 `build-sanity` 只在 PR 打开时校验的空档。

未完成：

- [x] `config.toml.sample` 与代码漂移（2026-09-16 核实结案：**无需变更**）。核实方法：列出代码中全部 koanf 读取点（`ko.String/Bool/Int/Unmarshal*`）并与数据库 `settings` 表的实际键比对（`SELECT split_part(key,'.',1), COUNT(*) FROM settings GROUP BY 1`）。结论：除 `app.address`、`app.admin_username`、`app.admin_password`（已弃用的遗留 API 用户）与 `[db].*` 外，文档中列出的每个键——`app.*` 的 21 个、`appearance`、`bounce`、`customer.custom_fields`、`maintenance.db`、`messengers`、`migrations`、`privacy`、`reply_ai`、`security`、`smtp`、`upload`——都存在于 `settings` 表，并由 `cmd/init.go:411` 的 `initSettings` 在启动时载入 koanf **覆盖**配置文件（`ko.Load(confmap.Provider(...))`，见 `cmd/init.go:431`）。因此 sample 只含 `[app]`/`[db]` 是设计使然，扩充它反而会诱导用户写入会被数据库覆盖的值。已做的修正：`docs/docs/content/configuration.md` 新增"仅存在于配置文件的键"表格与优先级说明，并把原先误导性的提示（`those sections have to be added to your config.toml by hand`）改为"数据库设置项不应写进 `config.toml`"。

## 已决策并实施：组织经理在公海的自助范围（2026-09-16）

决策：维持"**公海联系人维护仅最高管理员**"，组织经理只负责自己组织的公海分配归属与其回件邮箱。用户于 2026-09-16 在两选项中选定此项，本轮已实施。

背景与证据（保留供追溯）：公海分配创建/绑定恢复双路径后，公海联系人维护仍是最高管理员专属——`cmd/pools.go` 的 `AssignPoolContact`、`ImportOrgPoolAllocationMembers`、`RemovePoolContact`、`RestorePoolContact`（以及 `CreatePoolContact`、`ImportListIntoPool`）都经 `requirePoolAdministrator`。仓库自带回归脚本 `dev/pools_e2e_verify.ps1` 曾按 2026-09-04 的设计断言"组织经理可移除/恢复成员（200）"，实测返回 403；该守卫由提交 `4c12a5fb feat: refine business permissions and remove export center` 引入，脚本自该提交起即与代码不一致。

实施内容：

- [x] `dev/pools_e2e_verify.ps1`：成员维护断言改用最高管理员会话（`$root`，断言标签同步改为 "highest administrator …"），并新增三条反向断言锁定边界——组织经理的分配/移除/恢复一律 403。脚本另加登录前置校验：经理会话（`GET /api/pools/:id/allocations`）或管理员会话（`GET /api/settings`）未建立时立即抛出带指引的错误（分别提示加载 `dev/pools_e2e_seed.sql`、设置 `POOL_QA_SUPER_PASSWORD`），不再让失败表现为令人困惑的 403。
- [x] `docs/ARCHITECTURE.md` 在"一级公海与组织公海分配"一节新增"组织经理的公海分配自助边界"条目：逐条列出经理**可**做（创建/绑定本组织公海分配、维护其回件邮箱、读取本组织脱敏联系人）与**不可**做（单条新增、批量导入成员、逻辑移除与恢复、清除联系人邮箱、`/api/pools/import`）的操作，并写明各自的守卫。
- [x] `docs/harness/BUSINESS_LOGIC.md` 的剔除不变量由"组织管理员或最高管理员"更正为"仅最高管理员"，并说明它与"经理可创建/绑定"之间的分工：经理建立归属并维护回件邮箱，联系人主数据由最高管理员维护。

验证：脚本经 PowerShell 解析器语法检查通过；脚本新断言的每一条都在运行中的开发套件上实测成立——经理 assign/remove/restore 均 403、管理员 restore/remove 均 200、经理读取可见 `excluded=true` 且 `reason=e2e`、管理员用不存在的联系人移除返回 400、验证后 `org_pool_allocation_exclusions` 未恢复条数为 0（夹具已复原）。限制：本机不知道 `root` 账号口令，脚本未整机跑通，管理员侧断言是用遗留 Super Admin 令牌发出的等价请求验证的。

## 待办：API 文档的源码引用重新基线（2026-09-16）

背景：`docs/docs/content/apis/organizations.md` 在每节末尾以 `cmd/x.go:NNN` 形式给出证据出处。本轮独立审校确认其中一部分已经过时，并已修正 9 处引用：6 处 `cmd/handlers.go` 路由行号（路由表新增导致整体后移）与 3 处把 `internal/core/organizations.go` 误记为 `cmd/organizations.go` 的跨文件引用。但该页仍有约 40 处 `cmd/organizations.go:NNN` 与约 19 处 `internal/core/organizations.go:NNN` 引用**未重新基线**。

为何现在不做：工作区存在未提交的大规模改动——`cmd/organizations.go` 增加约 165 行、`internal/core/organizations.go` 在 86 行附近插入约 138 行、`cmd/handlers.go` +2 行；复核期间这些偏移仍在变动（`GetOrganizationRequests` 一度从 533 变为 536，`ArchiveOrganization` 581→584）。在代码定型前重新基线会立即失效。

- [ ] 待上述改动提交、工作区干净后，按当前文件重新基线该页全部 `cmd/organizations.go:NNN` / `internal/core/organizations.go:NNN` / `cmd/handlers.go:NNN` 引用：逐条按所在小节的端点或函数名定位真实行号，只改数字、不改正文。验收：随机抽查 10 条引用能直接命中对应代码；`python scripts/check_docs.py` 与 `mkdocs build --strict` 通过。
- [ ] 建议在 `docs/harness/README.md` 的维护规则补一句：涉及大规模行插入的代码变更（新增路由、整段函数搬迁）应顺带复核文档中的 `文件:行号` 引用，或改用函数名而非行号作为出处，以降低复发频率。
