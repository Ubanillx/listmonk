version: 1
display_name: listmonk OpenClaw Marketing
short_description: Run modular or end-to-end listmonk marketing workflows from OpenClaw using REST APIs and Bearer integration tokens.
default_prompt: Use the modular listmonk marketing skill to create or reuse a list, import subscribers, clone templates, reuse an existing campaign as a blueprint, create campaigns, update campaign status, or fetch analytics with Bearer integration tokens.
