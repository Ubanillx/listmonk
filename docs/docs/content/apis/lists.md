# Moved: CustomerLists API

The customer list API documentation is now published as the
[CustomerLists API](customer-lists.md) page.

This page only exists so that the previously published `/apis/lists/`
URL keeps working.
